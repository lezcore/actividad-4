// BIANOR SERGIO CHIPILE CARI

// Numeración sucesiva
for (i=0; i<=30; i++){
  console.log('Numeracion sucesiva ' + i);
}

// Numero maximo
function maxNumber(num1 , num2) {
  if ( num1 > num2){
    return num1;
  }
  return num2;
}
console.log('El numero máximo es ' + maxNumber(500,100));
